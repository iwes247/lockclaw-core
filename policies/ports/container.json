{
  "profile": "container",
  "description": "Default container profile — no published ports. All services loopback-only.",
  "allowed_ports": [],
  "notes": "In the default container profile, NO ports should be listening on non-loopback addresses. AI runtimes bind to 127.0.0.1 only. SSH is optional and must be explicitly enabled."
}
