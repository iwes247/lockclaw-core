{
  "profile": "container",
  "description": "Baseline v1 container profile. No published ports by default; runtime ports are loopback-only.",
  "allowed_ports": [22, 18789, 11434],
  "notes": "Used by pre-flight posture checks in hobby/builder modes. SSH remains opt-in."
}
